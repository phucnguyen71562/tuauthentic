import { deepOrange, red } from '@material-ui/core/colors';

const theme = {
  palette: {
    primary: {
      main: deepOrange[500],
    },
    colors: {
      gradient: 'linear-gradient(to right, #f83600, #fe8c00)',
      price: red[500],
    },
  },
};

export default theme;
